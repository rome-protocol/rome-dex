// Shared with store.ts (client) and store.test.mjs (node --test). Kept in .mjs
// so the test can import it without going through the TypeScript/React toolchain.
export const CHAIN_STORAGE_KEY = "rome-dex.chainId";
